<div class="container m-5">
  <div class="row mt-5">
    <h2 class="ml-5 px-4 bg-light shadow">商品詳細</h2>
  </div>

  <div class="row mt-5">
    <div class="col">
    <%= attachment_image_tag @item, :image, :fill, 400, 350, format:'jpeg', class: "float-right" %>
    </div>

    <div class="col">
      <table class="table table-borderless">
      　<tbody>
          <tr>
            <td>商品名</td>
            <td><%= @item.name %></td>
          </tr>
          <tr>
          　<td>商品説明</td>
          　<td><%= @item.introduction %></td>
          </tr>

          <tr>
          　<td class="text-nowrap">税込価格<br>
              (税抜価格)
          　</td>
          　<!-- to_s(:delimited)は3桁ごとにカンマ区切りで表示してくれるメソッド -->
          　<!-- add_tax_priceメソッドはitem.rbに記述 -->
          　<td><%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price %>)円</td>
          </tr>

          <tr>
          　<td class="text-nowrap">販売ステータス</td>
            <% if @item.sales_status == true %>
              <th style="color: green">販売中</th>
            <% else %>
              <th style="color: gray">販売停止中</th>
            <% end %>

          </tr>
        </tbody>
      </table>
    </div>
    </div>
    <div class="row">
      <div class="offset-sm-6 col-6">
    　 <%= link_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-success" %>
    　</div>
    </div>
  </div>
</div>
