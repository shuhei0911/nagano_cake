<div class='container'>
  <div class='row d-flex justify-content-center'>
    <div class="main-visual shadow">
      <div class="main-visual-text">
        <div class="text">ようこそ、ながのCAKEへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。
        </div>
      </div>
    </div>
  </div>
  <div class="row mt-4">
   <h1 class="font-weight-bold">新着商品</h1>
  </div>
  <div class="row mt-4">
    <% @items.each do |item| %>
     <div class="col">
      <p>
        <%= link_to item_path(item.id) do %>
          <%= attachment_image_tag item, :image, :fill, 250, 125, format:'jpeg'%>
        <% end %>
      </p>
      <p class="font-weight-bold"><%= link_to item.name, item_path(item.id), class: "item_#{item.id}" %></p>
      <p class="font-weight-bold">¥<%= item.price %></p>
     </div>
    <% end %>
  </div>
  <!--後で遷移先を商品一覧に変える-->
  <p class="text-right h4"><%= link_to '全ての商品をみる >' , items_path %></p>
</div>
